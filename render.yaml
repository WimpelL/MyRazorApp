services:
  - type: web
    name: my-razor-app
    runtime: dotnet
    buildCommand: "dotnet publish -c Release -o out"
    startCommand: "dotnet out/MyRazorApp.dll"
    plan: free
    envVars:
      - key: ASPNETCORE_URLS
        value: http://0.0.0.0:10000